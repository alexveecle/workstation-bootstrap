# Provision

```
sudo apt install -y curl
bash <(curl https://raw.githubusercontent.com/alexveecle/workstation-bootstrap/refs/heads/main/bootstrap)
```

## Incus testing

Use `./incus-setup.sh` to set up a VM from scratch.
Use `incus copy` to create/use a template.
